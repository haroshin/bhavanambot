version: '3.8'

services:
  bhavanam-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bhavanam-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
